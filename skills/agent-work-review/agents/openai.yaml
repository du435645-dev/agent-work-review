interface:
  display_name: "Agent Work Review"
  short_description: "Draft a grounded work review from multiple Agents and render local HTML"
  brand_color: "#1346D8"
  default_prompt: "Use $agent-work-review to inspect local state, collect and merge evidence from multiple Agents, draft and polish a grounded report, validate and save summary.json and summary.md, and render a standalone HTML presentation."

policy:
  allow_implicit_invocation: true
